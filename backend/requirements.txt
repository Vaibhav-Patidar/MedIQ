# MedIQ backend — hackathon runtime deps.
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
psycopg[binary]>=3.1
pydantic>=2.6
pydantic-settings>=2.2
neo4j>=5.19
PyJWT>=2.8
bcrypt>=4.1
python-multipart>=0.0.9
pandas>=2.2
numpy>=1.26

# --- test deps (kept in the same file so `docker compose exec backend pytest` works) ---
pytest>=8.0
httpx>=0.27

# --- OPTIONAL heavy ML deps ---
# The demo runs on the deterministic clinical surrogate in app/ml/inference.py and
# does NOT need these. Uncomment ONLY when loading a real trained TFT checkpoint
# (backend/checkpoints/, see README there); they add ~2GB+ to the image.
# torch>=2.2
# pytorch-forecasting>=1.1
# shap>=0.45
